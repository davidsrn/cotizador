<%= form_for @changeset, @action, fn f -> %>
  <%= if @changeset.action do %>
    <div class="alert alert-danger">
      <p>Oops, something went wrong! Please check the errors below.</p>
    </div>
  <% end %>

  <div class="form-group">
    <%= label f, :item, class: "control-label" %>
    <%= text_input f, :item, class: "form-control" %>
    <%= error_tag f, :item %>
  </div>

  <div class="form-group">
    <%= label f, :detail, class: "control-label" %>
    <%= text_input f, :detail, class: "form-control" %>
    <%= error_tag f, :detail %>
  </div>

  <div class="form-group">
    <%= label f, :monthly_cost, class: "control-label" %>
    <%= number_input f, :monthly_cost, step: "any", class: "form-control" %>
    <%= error_tag f, :monthly_cost %>
  </div>

  <div class="form-group">
    <%= label f, :date, class: "control-label" %>
    <%= date_select f, :date, class: "form-control" %>
    <%= error_tag f, :date %>
  </div>

  <div class="form-group">
    <%= label f, :location_id, class: "control-label" %>
    <%= select f, :location_id, Enum.map(@locations,
                                                fn
                                                  {k, v} -> {k, v}
                                                end) %>
    <%= error_tag f, :location_id %>
  </div>

  <div class="form-group">
    <%= submit "Submit", class: "btn btn-primary" %>
  </div>
<% end %>
